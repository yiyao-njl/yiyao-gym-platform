# 【体育馆运营管理后台系统】需求文档

## 一、后台系统核心用途 / 产品定位

- 本后台系统是体育馆运营管理系统的 B 端管理平台，面向超级管理员、店长、员工等运营人员使用。
- 系统基于 `Vue3 + Element Plus + Pinia + Vue Router + Axios + ECharts` 开发，后端对接 `Spring Boot + Spring Cloud` 微服务接口。
- 核心目标是提升体育馆日常管理效率，实现门店、场地、订单、支付、用户、营销、评价和经营数据的统一管理。
- 后台系统与 C 端微信小程序联动，小程序产生的预约、开场、支付、退款、评价等数据需在后台实时或准实时展示。
- 后台主要功能包括：
  - 管理员登录与权限控制
  - 首页经营数据看板
  - 门店与场地资源管理
  - 场地分类、营业时间、价格套餐配置
  - 预约订单、开场订单、退款申请管理
  - 用户、会员、优惠券、活动管理
  - 评价审核与回复管理
  - 收入、订单、场地使用率等统计分析
  - 系统日志、操作记录和基础参数配置

## 二、主要功能详情

### 2.1 全局基础布局

- 后台整体采用经典管理系统布局：
  - 左侧为固定侧边菜单栏。
  - 顶部为系统标题、折叠菜单按钮、当前门店、消息提醒、管理员信息和退出按钮。
  - 中间为主内容区，用于展示各业务页面。
  - 底部可展示版权信息或系统版本号。

- 左侧菜单建议包含：
  - 首页
  - 门店管理
  - 场地管理
  - 订单管理
  - 支付与退款
  - 用户管理
  - 会员管理
  - 优惠券管理
  - 活动管理
  - 评价管理
  - 数据统计
  - 系统管理

- 顶部按钮与交互：
  - 【折叠菜单】：点击后收起或展开左侧菜单。
  - 【门店切换】：店长或超级管理员可切换当前管理门店。
  - 【消息通知】：展示新订单、退款申请、异常支付、用户评价等提醒。
  - 【个人中心】：点击进入管理员资料页面。
  - 【退出登录】：点击后弹出确认框，确认后清除 Token 并跳转登录页。

- 登录状态规则：
  - 未登录用户访问后台任意页面时，自动跳转至登录页。
  - 登录成功后，根据角色动态生成可访问菜单。
  - Token 过期时，清除登录状态并提示“登录已过期，请重新登录”。
  - 无权限访问页面时，展示 403 无权限页面或返回首页。

- 跳转逻辑：
  - 登录成功后默认进入首页数据看板。
  - 点击左侧菜单切换对应业务页面。
  - 点击表格中的【查看详情】进入详情页或打开详情弹窗。
  - 新增、编辑操作优先使用弹窗表单；复杂配置页面可进入独立页面。
  - 删除、停用、退款审核等高风险操作必须二次确认。

### 2.2 首页功能

- 首页作为后台经营概览页面，主要展示体育馆运营核心数据。

- 页面内容：
  - 今日营业额
  - 今日订单数
  - 今日预约数
  - 今日开场数
  - 待处理退款数
  - 待审核评价数
  - 场地使用率
  - 热门运动类型
  - 近 7 日收入趋势
  - 近 7 日订单趋势
  - 各场地使用情况排行

- 核心组件：
  - 顶部数据卡片：展示收入、订单、用户、场地使用率等关键指标。
  - 折线图：使用 ECharts 展示收入和订单趋势。
  - 柱状图：展示不同运动类型或不同场地的使用情况。
  - 表格列表：展示最新订单、最新退款申请和最新用户评价。

- 页面按钮：
  - 【刷新】：重新请求首页统计数据。
  - 【查看订单】：跳转订单管理页面。
  - 【处理退款】：跳转支付与退款页面。
  - 【查看评价】：跳转评价管理页面。
  - 【导出数据】：导出当前统计数据为 Excel。

- 交互要求：
  - 数据卡片点击后可跳转到对应详情页面。
  - 图表支持按今日、近 7 日、近 30 日、本月筛选。
  - 鼠标悬停图表节点时展示具体数值。
  - 数据加载中显示骨架屏或 loading 状态。
  - 无数据时显示空状态，不展示空白页面。

### 2.3 我的页面功能

- 我的页面用于展示当前后台登录用户的信息、角色权限和账号操作。

- 未登录状态：
  - 后台系统不允许未登录访问我的页面。
  - 未登录访问时直接跳转至登录页。
  - 登录页包含账号输入框、密码输入框、验证码或图形校验、【登录】按钮。
  - 点击【登录】后调用 `/api/auth/admin/login` 接口，成功后保存 Token、用户信息和权限菜单。

- 已登录状态：
  - 展示管理员头像、姓名、账号、手机号、角色、所属门店、最近登录时间。
  - 展示当前角色拥有的权限范围，例如门店管理、订单管理、退款审核、数据统计等。
  - 支持修改个人资料，包括头像、昵称、手机号。
  - 支持修改密码，修改时需输入原密码、新密码和确认密码。
  - 支持查看登录日志和近期操作记录。

- 页面按钮：
  - 【编辑资料】：打开资料编辑弹窗。
  - 【修改密码】：打开修改密码弹窗。
  - 【查看操作日志】：跳转系统日志页面。
  - 【退出登录】：弹出确认框，确认后退出后台。

- 跳转逻辑：
  - 资料修改成功后刷新当前用户信息。
  - 修改密码成功后建议重新登录。
  - 点击退出登录后清除 Token、Pinia 用户状态和动态路由，返回登录页。

### 2.4 核心操作功能（场馆运营管理）

- 核心操作功能围绕“门店配置 - 场地配置 - 用户预约 - 支付订单 - 后台处理 - 数据统计”展开。

- 门店管理：
  - 页面展示门店名称、城市、地址、联系电话、营业状态、营业时间、创建时间。
  - 支持按城市、门店名称、营业状态筛选。
  - 按钮包括【新增门店】、【编辑】、【营业中/休息中切换】、【停用】、【查看详情】。
  - 新增或编辑门店时需填写门店名称、城市、地址、经纬度、联系电话、营业时间、门店图片、默认营业状态。
  - 门店营业状态必须由店铺员工、店长账号或具备门店管理权限的管理员在后台显式维护，支持设置为“营业中”“休息中”“停用/维护”等状态。
  - 营业时间仅表示门店理论营业时段，不应单独决定前端展示的营业状态；未到营业时间且后台未手动开店时，小程序门店列表必须显示“休息中”，不能显示“营业中”。
  - 店铺员工可在授权门店范围内执行“开始营业”“暂停营业/休息中”“恢复营业”等日常状态操作，操作后需实时或准实时同步到 C 端小程序门店选择页、首页推荐和场地/下单校验。
  - 门店营业状态变更需记录操作人、操作时间、变更前状态、变更后状态和备注，便于追踪误操作。
  - 停用门店前需校验是否存在未完成订单。

- 场地管理：
  - 页面展示场地编号、场地名称、运动类型、所属门店、价格、状态、图片、可预约时间。
  - 场地状态包括空闲、使用中、已预定、维护中、已停用。
  - 按钮包括【新增场地】、【编辑】、【配置套餐】、【维护】、【停用】。
  - 管理员可配置篮球、羽毛球、乒乓球、气排球、匹克球等场地分类。
  - 修改场地状态后，小程序端应同步刷新场地可用状态。

- 营业时间与套餐价格：
  - 支持按门店配置营业时间、可预约天数、最小预约单位、最大预约时长。
  - 支持配置固定套餐和自定义时长价格。
  - 套餐字段包括套餐名称、时长、原价、优惠价、适用场地、适用日期、是否启用。
  - 修改价格后，新订单使用新价格，历史订单保留原成交价。

- 订单管理：
  - 页面分为预约订单和开场订单。
  - 表格展示订单号、用户、手机号、门店、场地、预约时间、订单金额、支付状态、订单状态、创建时间。
  - 支持按订单号、手机号、门店、场地、订单状态、时间范围筛选。
  - 操作按钮包括【查看详情】、【取消订单】、【确认到场】、【申请退款】、【再次预约】。
  - 点击订单详情后展示场地信息、用户信息、支付流水、优惠信息和操作记录。
  - 取消订单、退款审核等操作必须二次确认并记录操作日志。

- 支付与退款：
  - 页面展示支付单号、订单号、支付金额、支付方式、支付状态、退款状态、支付时间。
  - 支持查看微信支付回调结果和退款流水。
  - 退款申请需支持【同意退款】和【拒绝退款】。
  - 审核退款时必须填写审核备注。
  - 支付回调、退款回调必须保证幂等，后台只展示最终可信状态。

- 用户与会员管理：
  - 用户列表展示昵称、手机号、会员等级、积分/经验值、优惠券数量、注册时间、状态。
  - 支持按手机号、昵称、会员等级、账号状态筛选。
  - 支持查看用户订单、优惠券、积分流水和评价记录。
  - 可对异常用户进行禁用或恢复。
  - 会员管理支持配置会员等级、权益说明、折扣规则和成长值规则。

- 优惠券与活动管理：
  - 优惠券字段包括名称、面额、使用门槛、适用门店、适用场地、发放数量、有效期、状态。
  - 活动字段包括活动标题、封面图、活动时间、活动规则、跳转类型、上下架状态。
  - 按钮包括【新增】、【编辑】、【上架】、【下架】、【删除】。
  - 已被用户领取或使用的优惠券模板不允许物理删除，只能停用。

- 评价管理：
  - 展示用户评价内容、评分、图片、关联订单、关联场地、提交时间、审核状态。
  - 支持后台回复、隐藏违规评价、恢复展示。
  - 对违规内容操作需记录管理员和操作时间。

- 数据统计：
  - 使用 ECharts 展示收入趋势、订单趋势、场地使用率、用户增长、运动类型占比。
  - 支持按日、周、月、自定义时间范围筛选。
  - 支持按门店、场地类型、场地编号过滤。
  - 支持导出 Excel 报表。

## 三、界面与使用体验要求

### 3.1 界面样式

- 后台整体采用简洁、专业、清晰的管理系统风格。
- 推荐使用 Element Plus 作为基础组件库，保证表格、表单、弹窗、分页、按钮样式统一。
- 主色建议使用蓝色或绿色，体现运动、健康、运营管理属性。
- 页面布局应以数据管理效率为主，避免过度装饰。
- 表格页面需保持字段对齐清晰，重要状态使用颜色标签区分。
- 常见状态颜色建议：
  - 成功 / 已支付 / 可用：绿色
  - 待处理 / 待支付：橙色
  - 失败 / 已取消 / 已停用：红色
  - 普通 / 已完成：蓝色或灰色

### 3.2 交互体验

- 表单必须提供必填校验、格式校验和错误提示。
- 新增、编辑、删除、停用、退款等操作需提供明确反馈。
- 危险操作必须二次确认。
- 表格支持分页、筛选、排序、重置查询。
- 弹窗关闭前如存在未保存内容，应提示用户确认是否离开。
- 登录过期时自动跳转登录页，并提示重新登录。
- 接口请求失败时展示后端返回的 `message`，不要只显示“系统错误”。
- 按钮点击后应进入 loading 状态，避免重复提交。

### 3.3 性能速度

- 首页数据看板首屏加载时间建议控制在 2 秒以内。
- 普通列表查询响应时间建议控制在 1 秒以内。
- 大表格数据必须分页加载，不一次性拉取全部数据。
- 路由页面建议按需加载，减少首屏包体积。
- ECharts 图表数据量较大时，应按时间范围聚合后展示。
- 高频数据如场地状态、订单状态可通过定时刷新或 WebSocket 更新。
- 静态资源应进行压缩，生产环境开启 gzip 或 brotli。

### 3.4 安全

- 后台所有接口必须经过登录认证。
- 使用 JWT Token 进行身份校验，Token 过期后必须重新登录或刷新 Token。
- 前端根据权限动态生成菜单，后端也必须进行接口级权限校验。
- 不同角色的数据范围必须隔离：
  - 超级管理员可查看全部门店。
  - 店长只能查看授权门店。
  - 员工只能操作被授权的业务模块。
- 门店营业状态属于门店运营控制项，只有超级管理员、店长或被授权员工可修改；普通员工只能操作自己所属或被授权门店。
- 密码不得明文存储，后端应使用 BCrypt 加密。
- 支付金额、订单状态、退款状态必须以后端计算和校验结果为准。
- 后台不得暴露用户密码、支付密钥、微信支付证书、数据库内部字段等敏感信息。
- 关键操作必须写入操作日志，包括新增、编辑、删除、停用、营业状态切换、退款审核、权限变更。

## 四、核心功能优化建议

- 优化目标：
  - 提升体育馆管理人员处理订单、场地和退款的效率。
  - 降低人工沟通和手工登记成本。
  - 保证场地预约、支付、退款、库存状态的数据一致性。
  - 提升后台数据统计能力，为经营决策提供依据。
  - 让后台系统与微信小程序形成完整业务闭环。

- 具体优化方案：
  - 建立统一 request 封装，集中处理 Token、错误码、loading、重复提交和接口超时。
  - 使用 Pinia 管理管理员信息、权限菜单、当前门店、全局字典和系统配置。
  - 使用 Vue Router 动态路由，根据后端返回权限生成菜单。
  - 场地状态采用颜色标签和时间轴展示，方便管理员快速判断空闲、占用、维护状态。
  - 订单详情页增加操作时间线，展示创建订单、支付成功、到场、退款、取消等完整流程。
  - 退款审核增加备注模板，提升处理效率并保证记录规范。
  - 首页看板增加快捷入口，例如“待处理退款”“待审核评价”“今日预约订单”。
  - 对门店、场地、套餐、优惠券等配置类数据增加启用/停用状态，避免误删历史数据。
  - 门店列表和门店详情接口需返回后台维护的实时营业状态、理论营业时间和当前是否可下单原因；小程序展示“营业中/休息中”应优先以后台状态为准，再结合营业时间做下单校验。
  - 对预约下单和场地占用状态使用后端 Redis 锁 + MySQL 唯一约束兜底，防止重复预约。
  - 支付回调、退款回调、订单状态更新通过 RabbitMQ 异步处理，提高系统稳定性。
  - 后台统计数据可通过定时任务预聚合，减少实时查询压力。
  - 增加操作日志和异常日志页面，便于定位问题和追踪责任。

## 五、设计风格

- 整体风格：简洁、专业、高效、偏企业级后台管理风格。
- 技术风格：Vue3 组合式 API + Element Plus 组件化开发。
- 页面风格：左侧菜单 + 顶部导航 + 内容区卡片 / 表格布局。
- 色彩风格：以白色、浅灰为背景，搭配蓝色或绿色作为主色，突出体育和运营管理属性。
- 数据展示：重要经营指标使用卡片展示，趋势类数据使用 ECharts 图表展示，业务明细使用表格展示。
- 操作风格：常规操作轻量化，危险操作强确认，复杂操作流程化。
- 文案风格：按钮和提示语简洁明确，例如“新增场地”“确认退款”“订单已取消”“登录已过期，请重新登录”。
- 适配要求：优先适配 PC 管理端，同时保证 1366px、1440px、1920px 等常见桌面分辨率下布局正常。

## 六、当前开发同步说明（2026-05-18）

- 已新增独立开发文档：`admin-web/体育馆运营管理后台-前端开发文档.md`，用于记录技术栈、目录、页面、接口规则和开发检查清单。
- 当前后台真实代码位于根目录 `admin-web/`，技术栈为 Vue3、Vite、Element Plus、Pinia、Vue Router、Axios、ECharts。
- `src/api/request.js` 统一处理 `/api` 请求、JWT、`X-Client-Type: admin-web`、统一响应和 401 退出登录。
- `src/api/modules.js` 是后台接口聚合入口；新增页面或功能时应优先在这里补接口方法。
- `src/data/mock.js` 已清空为结构占位，接口失败时不再展示假业务数据；新增业务不要恢复假数据兜底。
- 默认后台账号来自 MySQL `admin_account` 表，开发期为 `admin / admin123`、`manager / admin123`、`staff / admin123`，生产必须替换为安全密码和完整权限审计。
